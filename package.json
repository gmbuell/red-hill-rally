{
  "name": "red-hill-rally",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "wrangler dev",
    "predeploy": "npm test && wrangler d1 migrations apply red-hill-rally --remote",
    "deploy": "wrangler deploy",
    "preview": "wrangler d1 migrations apply red-hill-rally-preview --env preview --remote && wrangler versions upload --env preview",
    "preview:deploy": "wrangler d1 migrations apply red-hill-rally-preview --env preview --remote && wrangler deploy --env preview",
    "test": "vitest run",
    "audit": "node scripts/lighthouse.mjs",
    "wcag": "node scripts/wcag.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "axe-core": "^4.13.0",
    "lighthouse": "^13.4.1",
    "puppeteer-core": "^25.9.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "allowScripts": { "esbuild": true, "workerd": true }
}
