Campaign action button in Bebas Neue caps with square corners; hover inverts.

```jsx
<Button variant="accent" size="lg">Donate now.</Button>
```

Variants: primary (black), accent (red), secondary (outline), ghost (underlined text). Sizes sm/md/lg.